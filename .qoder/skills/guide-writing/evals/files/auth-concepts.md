# Authentication concepts

This document defines authorization servers, clients, resource owners,
redirect URIs, authorization codes, access tokens, refresh tokens, scopes,
state, nonce, and PKCE. New guides should link here instead of reproducing
the full vocabulary.

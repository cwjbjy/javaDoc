# OAuth provider operations

This runbook covers provider registration, client-secret rotation, redirect
URI changes, monitoring, incident response, rollback, and emergency access.
Implementation guides should link here instead of copying operational steps.
